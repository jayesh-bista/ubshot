# UbShot Requirements

# GUI Framework (Qt6 for Python)
PySide6>=6.5.0

# Global hotkeys for X11 (captures keyboard events system-wide)
pynput>=1.7.6

# Image processing (blur, inpainting)
opencv-python-headless>=4.8.0

# NumPy (required by opencv)
numpy>=1.24.0

# OCR support
pytesseract>=0.3.10
Pillow>=10.0.0
